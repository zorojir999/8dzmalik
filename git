git init - инициализация
git remote add origin link - привязка репозитория



git add .
git commit -m 'результат'
git push origin master/main



git clone link
git remote remove origin
git rm --cached -r namedir



git branch


git branch - проверить ветки
git branch name - создает ее
git checkout name- переключается на ветку
git checkout -b name- создает и переключается
git merge слияние веток